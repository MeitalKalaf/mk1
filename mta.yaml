ID: mk1
_schema-version: '2.0'
version: 0.0.1

modules:
  - name: hdb1
    type: hdb
    path: hdb1
    requires:
      - name: hdi-container

resources:
  - name: hdi-container
    properties:
      hdi-container-name: '${service-name}'
    type: com.sap.xs.hdi-container
